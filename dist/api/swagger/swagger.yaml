swagger: "2.0"
info:
  version: "1.0.0"
  title: NODE JS API
host: localhost:4000
basePath: /
schemes:
  - http
consumes:
  - application/json
produces: -application/json
paths:
  /employee:
    get:
      name: String
      salary: numeric
